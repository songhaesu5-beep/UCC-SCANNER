/*! html5-qrcode - v2.3.9 */
// 이 파일은 html5-qrcode 라이브러리를 사용하는 데 필요한 스크립트입니다.
// 실제 내용은 https://unpkg.com/html5-qrcode@2.3.9/minified/html5-qrcode.min.js 를 참조하십시오.
// 아래는 템플릿 예시이며, 전체 라이브러리는 웹에서 복사해 붙여넣어야 완전합니다.

class Html5Qrcode {
  constructor(elementId) {
    this.elementId = elementId;
  }
  start(config, settings, onSuccess, onError) {
    console.log("Starting dummy QR scanner...");
  }
  stop() {
    return Promise.resolve();
  }
}
